package com.ailms.entity.enums;

/** Ngữ cảnh hội thoại do backend suy ra từ tầng trách nhiệm của user. */
public enum AiConversationScope {
    ADMIN_COPILOT,
    EMPLOYEE_COPILOT,
    STUDENT_ASSISTANT,
    COURSE_ASSISTANT
}
